# 20260511

# 根节点无法改变类型 OK

# 插件增加如果没有选中任何数据 也要能直接打开一个空object

# 3 拖拽填充 不能带着数据类型

["apple", "banana", "cherry", "date", "elderberry", "11", 11, 11, 11]

# 单元格没回车 就去点其他功能，要强制更新当前活动单元格 否则最新数据丢失。

比如 没回车 就去拖拽 ，还是旧值

# 支持纯文本后，js 变量无法识别成 json 识别成了空object

js ts文件的变量识别功能要保留。
其他文件识别成空object 是对的。
